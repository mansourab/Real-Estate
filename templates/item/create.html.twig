{% extends 'profil/partials/base.html.twig' %}

{% block title 'Create an Item'%}

{% block body %}
    
    <h3 class="text-center mt-4 mb-2">Add new item</h3>
	<div class="col-md-6 m-auto">
	    {{ form_start(form) }}
	        {{ form_row(form.title) }}
	        {{ form_row(form.description) }}
			{{ form_row(form.category) }}
			{# {{ form_row(form.imageFile) }} #}

	        <button type="submit" class="btn btn-primary btn-block btn-lg">add new one</button>
	    {{ form_end(form) }}
	</div>
{% endblock %}
